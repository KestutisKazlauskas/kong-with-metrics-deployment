---
apiVersion: v1
kind: Service
metadata:
  namespace: apis
  name: billing
  labels:
    app: billing
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: httpbin
---
apiVersion: v1
kind: Service
metadata:
  namespace: apis
  name: invoice
  labels:
    app: invoice
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: httpbin
---
apiVersion: v1
kind: Service
metadata:
  namespace: apis
  name: comments
  labels:
    app: comments
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: httpbin
